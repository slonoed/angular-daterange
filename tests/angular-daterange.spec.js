'use strict';
describe('daterange', function() {
    beforeEach(function () {
        angular.module('MyApp.directives', [])
    })

    it('must bu work', function() {
        expect(1).toBe(1)
    });
});